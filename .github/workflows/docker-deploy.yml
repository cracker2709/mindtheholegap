---
name: test
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world action
        run: echo "Hello, world!"
# name: Build & Deploy to Cloud Run

# on:
#   release:
#     types: [ published ]

# permissions: write-all

# env:
#   PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
#   REGION: europe-west1
#   SERVICE_NAME: cracker2709
#   IMAGE_NAME: mindtheholegap

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Authenticate to Google Cloud
#         uses: google-github-actions/auth@v2
#         with:
#           credentials_json: ${{ secrets.GCP_SA_KEY }}

#       - name: Set up Cloud SDK
#         uses: google-github-actions/setup-gcloud@v2

#       - name: Configure Docker to use gcloud as credential helper
#         run: |
#           gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev

#       - name: Build and push Docker image
#         run: |
#           docker build -t ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.SERVICE_NAME }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }} .
#           docker push ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.SERVICE_NAME }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}

#       - name: Deploy to Cloud Run
#         run: |
#           gcloud run deploy ${{ env.PROJECT_ID }} \
#             --image ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.SERVICE_NAME }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }} \
#             --region ${{ env.REGION }} \
#             --platform managed \
#             --allow-unauthenticated

